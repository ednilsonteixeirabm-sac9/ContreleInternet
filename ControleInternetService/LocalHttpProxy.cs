using ControleInternet.Common;

namespace ControleInternet.Service
{
    // Implementado na etapa seguinte. A API já permite ao serviço preservar
    // a ordem segura: iniciar o listener antes de ativar o proxy do Windows.
    internal sealed class LocalHttpProxy
    {
        public void Start(AppConfig config)
        {
        }

        public void Update(AppConfig config)
        {
        }

        public void Stop()
        {
        }
    }
}
